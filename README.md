# SCT_CS_4
This project is a safe key‑input logger that records only the keys typed inside the program’s own window. It logs each key press into a file and helps users understand keyboard event handling in Python without capturing system-wide keystrokes.
